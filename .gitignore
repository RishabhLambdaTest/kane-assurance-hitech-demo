# Kane CLI — commit the assurance graph and the designed suite.
# Ignore run artifacts: evidence packs are 2-11 MB each of screenshots and
# traces, produced fresh by every run and uploaded by CI as an artifact.
.context/locks/
.context/sessions/
.testmuai/evidence/

# Pipeline outputs (CI artifacts, not source)
coverage-report.json
coverage-after-design.json
run-summary.json
regression-summary.json
selected-tests.txt
evidence-packs/
merge-report.json
invalid-packs.json
validation-report.json
context-graph.html
public/

# App build
app/node_modules/
app/.next/
app/out/

# Logs / OS / Python
*.log
.DS_Store
Thumbs.db
__pycache__/
*.pyc
node_modules/
